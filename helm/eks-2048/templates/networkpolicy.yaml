apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: eks-2048-network-policy
spec:
  podSelector:
    matchLabels:
      app: eks-2048
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: ingress-nginx
      ports:
        - protocol: TCP
          port: 80
