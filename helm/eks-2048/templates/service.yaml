apiVersion: v1
kind: Service

metadata:
  name: eks-2048-service

spec:
  selector:
    app: eks-2048

  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 80

  type: {{ .Values.service.type }}
